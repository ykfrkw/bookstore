# 設計仕様

## 解きたい問題

研究者が本を買うとき、Amazon で見つけてそのまま Amazon で買ってしまう。しかし
実際には、(a) 地元書店で取り寄せれば書店にお金が落ちる、(b) 大学生協なら
研究費で買えて事務処理も通しやすい、という選択肢がある。にもかかわらず、
その 2 つは「Amazon のページから離れて、別のサイトを開いて、書名を打ち直す」
必要があるため選ばれない。

**摩擦の差だけで購買先が決まってしまっている。** ここを埋める。

摩擦を埋めるのが目的なので、**このツール自体の摩擦が数クリックを超えたら本末転倒**
になる。以下の「注文導線」「UI / デザインシステム」は、その制約から逆算して決めている。

## スコープ

### v0.1（実装済み）

- Amazon の書籍ページに注文パネルを注入する
- ISBN を確定し、openBD で書誌を引く
- 生協ルート / 地元書店ルートの注文メール下書きを作る
- 生協ルートでは研究費 / 私費を切り替え、財源（科研費等）と予算代表者を本文に入れる
- 複数冊をまとめて 1 通にする
- 同じロジックがローカルページでも使える

### v0.2（実装済み・2026-08-07）

対応サイトを拡大した。「Amazon のページを改変する拡張」ではなく
「オンライン書店の書籍ページに生協・地元書店ルートを添える拡張」にするのが狙い。

1. 紀伊國屋ウェブストア — URL に ISBN 直埋め込み（`/f/dsg-01-<ISBN13>`）。DOM 依存ほぼゼロ
2. 丸善ジュンク堂ネットストア — 同じく URL 直埋め込み（`/products/<ISBN13>`）
3. 楽天ブックス — URL は内部 ID だが meta タグ・title・本文の 3 経路で ISBN が取れる

honto は 2024 年に紙の通販を終了しているため対象外（後継の丸善ジュンク堂で代替）。
紀伊國屋・丸善ジュンク堂は書誌セレクタを実査していないので、`fallback` は空にして
openBD に任せている（アンカーが外れたら floating 表示になるだけで壊れない）。

### スコープ外（意図的に外している）

- 自動発注・自動ログイン（→ `CLAUDE.md` の禁止事項）
- 図書館蔵書チェック（v0.4 以降）
- 在庫・価格の比較

## なぜ「メール」なのか

当初は各サービスへのディープリンクを考えたが、メールに一本化した。

- **独立系書店には API も注文フォームも無い。** 一番応援したい相手ほど
  デジタル導線が無い。メールなら相手を選ばない。
- **大学生協の公費注文は大学ごとに運用が違う。** 備考欄に財源を書く方式、
  独自の公費 WEB フォーム、外商担当へのメール、と分かれている
  （`docs/research.md` 参照）。最大公約数はメールだった。
- **人の確認が挟まる。** 研究費の執行を伴うので、これは欠点ではなく利点。

ディープリンクは後から「メールの代わりに使える出力先」として足せる設計に
してある（`composeOrder` の戻り値を差し替えれば済む）。

## 注文導線 — 情報量の多い経路から順に選ぶ

### 前提: フル版の和文はいかなる場合も mailto に載らない

日本語 1 文字はパーセントエンコードで 9 バイトになる。実測上の安全圏は
URL 全体で 2000 文字前後（`MAILTO_SAFE_LENGTH`）なので、**フル版の注文メールは
1 冊でも必ず超える**。

当初はここから「本文入り mailto は死に経路なので分岐ごと捨てる」と決めていたが、
**その前提は実測で覆った**。効いているのは書名の長さではなく、挨拶・結びの定型文、
罫線、著者・出版社・発行年、合計行といった**周辺の飾り**である。それらを削った
簡略版（`composeOrder({ compact: true })`）なら収まる。実測（2026-09-02）:

| ケース | 本文 | encoded | 上限 2000 に対して |
| --- | --- | --- | --- |
| フル / 生協 / 研究費 / 和書 1 冊 | 519 字 | 2738 | 超過 +738 |
| **簡略 / 生協 / 研究費 / 和書 1 冊** | 242 字 | **1546** | 残 454 |
| フル / 書店 / 和書 1 冊 | 481 字 | 2547 | 超過 +547 |
| **簡略 / 書店 / 和書 1 冊** | 209 字 | **1410** | 残 590 |
| 簡略 / 生協 / 研究費 / 3 点 | 344 字 | 1990 | 残 10 |
| 簡略 / 生協 / 研究費 / 4 点 | 395 字 | 2212 | 超過 +212 |

いずれも全項目を埋めたプロフィール（組合員番号・会員番号を含む）での実測。
番号を登録していなければ各 60 字ほど余裕が増える（書店 1 冊で 1360 / 残 640）。
複数点は同じ書名に「第 n 巻」を付けたもの。

**書名は削らない。** ISBN が 1 桁違ったときに人が気づける唯一の手がかりであり、
誤発注防止として実際に効いている。削るのは飾りだけ。

**利用者が自分で入力した項目も削らない。** 組合員番号・会員番号
（`memberNumber`）は既定が空の任意項目で、埋まっているのは「自分の生協・書店が
この番号を求める」という明示的な入力である。簡略版で黙って落とすと注文が
通らなくなる。削って良いのは、コードが勝手に足している飾りだけ。

### 決定: 3 段階で選ぶ

3 面のメール導線（注入パネル / popup / ローカル版）は `pickMailPlan` を通して
次の順に判定し、**最初に収まったものを使う**。

1. **フル版が収まる** → フル版の本文入り `mailto` で開く（極小プロフィールのみ。稀）
2. **収まらないが簡略版が収まる** → **簡略版の本文入り `mailto` で開く**。貼り付け不要
3. **どちらも収まらない** → **フル版の本文**をコピーし `mailtoHeaderOnly` で開く

これを踏まえて、次のように定める。

1. **判定は必ず `buildMailto` の `tooLongForMailto` を通す。** 長さ判定を
   UI 側に二重実装しない。3 段階の選択そのものも `compose.js` の
   `pickMailPlan`（純関数）に置き、注入パネル / popup / ローカル版の 3 面が
   同じ判定を書き写さないようにする。
2. **件数で決め打ちしない。** 「1 冊のときだけ簡略版」といった分岐は書かない。
   長さで判定すれば、実際には 3 点程度まで簡略版で収まる（上表）。書名の長さや
   プロフィールの分量で境界は動くので、そこを人が予測して固定しない。
3. **クリップボードに入れるのは常にフル版の本文だけ。** コピー経路には長さ制限が
   無いので、情報量の多い方を渡す。貼り付け先がメーラーの本文欄であることが
   確定しているため、`To:` / `Subject:` 行が混ざると手で消す手間が増える。
   `plain`（ヘッダ込み）を使うのは、メーラーを開かずに Slack 等へ貼る用の
   二次アクション「全文をコピー」だけ（これはフル版のまま変えない）。
4. **経路 2 で開いたことは必ず利用者に伝える。** 簡略版は貼り付けが要らない
   代わりに書誌が減る。黙って差し替えると「いつもと文面が違う」だけが残る。
   **popup では押した後に書いても読めない**（`chrome.tabs.create` で popup が
   即座に閉じるため）。メールボタンの近くに「本文入りで開きます（簡略版）」/
   「本文をコピーして開きます」を**押す前**に静的に出し、カートの増減・注文先・
   支払区分の変更で評価し直す。
5. **クリップボード書き込みは必ずフィードバックを出す。** コピーは画面上で
   不可視の副作用なので、成功したか分からないと同じボタンを二度押される。
   toast（`role="status"` / `aria-live="polite"`）＋ボタンラベルの一時変更で示す。
6. **コピーはメーラーを開く前に実行する。** `window.open` でフォーカスが移ると
   `navigator.clipboard.writeText` が拒否されうる。この順序を入れ替えない。
   経路 1・2 では何もコピーしない。3 面が `pickMailPlan` を通していることと、
   コピーが `window.open` / `chrome.tabs.create` / `location.href` より前に
   あることは `test/extension-source.test.mjs` がソース文字列で固定している。
7. **プレビューを持つ面は、実際に送られる本文を見せる。** 本文 textarea を持つのは
   ローカル版だけで、そこにフル版を出して簡略版を送ると、利用者が画面で校正した
   ものと違う文面が飛ぶ。`compose()` の時点で `pickMailPlan` まで走らせ、
   選ばれた本文を textarea に出し、経路の案内もそこで出す（クリック前に気づける）。

### 簡略版を使わない場合

**利用者が自分で書いた文章がある下書きでは簡略版を使わない。** 簡略版は
「ひとこと」（`message`）と明細の備考（`item.note`）を落とすため、黙って差し替えると
利用者の文章が消える。これは実行時エラーにならず気づけない種類の事故なので、
UI の分岐ではなく **`composeOrder` 側で担保する**（`compact: true` を指定されても
自由記述があればフル版を返す）。フル版に戻れば長さ制限を超えるので、UI は
何もしなくても経路 3 に落ちる。

ローカル版はさらに、**本文 textarea が手編集されている場合も簡略版を候補にしない**
（簡略版は生成物なので、差し替えると編集を捨てることになる）。この場合は
`pickMailPlan` に `compact: null` を渡す。

### 文面は `templates` に置く

簡略版の挨拶・結びも `profile.templates` に置く（`coopCompactGreeting` /
`coopCompactClosing` / `bookstoreCompactGreeting` / `bookstoreCompactClosing`）。
大学ごとに決まり文句が違うため、コードに直書きしない。`withDefaults` が
既定値をマージするので、既に設定を保存済みの利用者にも自動で入る。

**ここを長くすると簡略版まで制限を超えてコピー経路に落ちる。** 上表の余裕
（生協で 454・書店で 590）が予算であることを忘れないこと。この余裕は
`test/compose.test.mjs` の `COMPACT_MIN_HEADROOM`（300 字）で下限を固定してあり、
テンプレートを伸ばしすぎるとテストが落ちる。

### 主導線のボタン階層（注入パネル）

注入パネルの主導線は、Amazon の購入ボックスと同じ**縦積み・幅いっぱいのピル 2 つ**。
上が「カートに入れる」（outline）、下が「今すぐ注文メール」（primary）。

| ボタン | 強調 | 中身 |
| --- | --- | --- |
| カートに入れる | outline | カートに追加。複数冊を 1 通にする |
| 今すぐ注文メール | primary | この 1 冊で即メール作成（→「決定」の 1 クリック） |

- **カートは二次アクションではなく主導線側に置く。** 「今この 1 冊」と
  「あとでまとめて」は購入意思の 2 分岐であって、片方が低強調の補助操作ではない。
  Amazon の購入ボックス（カート / 今すぐ買う）と同じ選択構造なので、
  そこに合わせた方が説明不要で読める。
- **形は寄せるが色は寄せない。** 対応サイトが 4 つあり、Amazon の黄・橙は
  他 3 サイトで浮く。ブランドブルーの `--primary` 規定を維持する。
- **「今すぐ買う」という語は使わない。** 実際には購入せずメール下書きを作るだけで、
  Amazon 本物の購入ボタンがすぐ近くにある。同じ語だと双方向の誤クリックを招く。
- 「1 画面の primary は 1 つだけ」は維持（primary は「今すぐ注文メール」のみ）。

### 二次アクション

一次導線から降ろすが、消さないもの。低強調（ghost）で置く。

| アクション | 中身 | 用途 |
| --- | --- | --- |
| 全文をコピー | `plain`（To/Subject/本文） | メーラーを使わず貼る |
| 備考欄をコピー | `remarks` 1 行 | 生協の公費 WEB フォーム |

### ローカル版の導線

ローカル版には「注文メールを作る」ボタンがあり、押すまで本文が生成されない。
これは中間ステップとして無駄なので、**注文先・支払区分・冊数・ひとことの変更で
本文を自動再生成**し、ボタンは「メールを作る（＝コピーして開く）」に一本化する。
本文 textarea の手編集は引き続き尊重する（`currentDraft` が押下時点の値で
mailto を組み直す方針は維持）。

## メールの開き方（v0.4・2026-09-04）

### 問題: 押しても何も起きない

Chrome は `mailto:` を「登録済みのプロトコルハンドラ → OS の既定メーラー」の
順に渡す。**どちらも無いと無反応になる。** 利用者から見ると拡張の不具合と
区別がつかない（エラーも出ない）。実機で確認したところ、Gmail が
`chrome://settings/handlers` で `mailto` のハンドラとして登録されていなかった。

**ハンドラの登録状況は API から照会できない**（`registerProtocolHandler` は
書けても読めない）。だから「開けたか」は推定するしかなく、推定は外れうる。
本命の価値は推定ではなく**退路（Gmail の作成画面を直接開く）**の方にある。

### 否定された仮説: 長さ

当初は「Gmail をハンドラにすると `mailto:` 全体が `url=` に再エンコードされ、
長さ制限を超えて落ちている」と考えたが、**実測で否定された**。

- 二重エンコードの膨張率は **1.63 倍**（`%` 1 文字 → `%25` の 3 文字なので ×3
  ではない。既に ASCII の部分は増えない）
- 簡略版の Gmail URL 全体で **2,936 文字**。Gmail の実効上限にもブラウザの
  上限にも届かない
- 生協・研究費・和書 1 冊（経路 2 = 簡略版）の実測は mailto 1,425 文字 /
  Gmail 1,463 文字

したがって **`MAILTO_SAFE_LENGTH`（2000）は変えない。** 3 段階の選択
（`pickMailPlan`）にも手を入れない。ここを「長さの問題」として直そうとすると、
実際の原因（ハンドラ未登録）は残ったまま経路だけが壊れる。

### 設定: `defaults.mailOpener`

| 値 | 挙動 |
| --- | --- |
| `auto`（既定） | まず `mailto:` を試し、開けなかったと推定できたときだけ退路を出す |
| `mailto` | 既定のメーラーに固定。**退路は出さない**（意図的な選択を押し返さない） |
| `gmail` | Gmail の作成画面を新しいタブで開く |

判断は `src/core/mailopen.js` に置く（`resolveMailTarget` / `looksUnopened` /
`buildGmailCompose`）。3 面はどれも「どの URL を開くか」を自分で決めない。

### 面ごとの開き方

| 面 | 開き方 | 新タブ | 推定 | 退路 |
| --- | --- | --- | --- | --- |
| 注入パネル | `<a>` を作って click（**target なし**） | しない | する | 推定が外れたときだけ＋学習 |
| popup | `chrome.tabs.create` | する | 不可 | 出さない（設定に従う） |
| ローカル版 | `<a target="_blank" rel="noopener">` | する | しない | **常設リンク** |

**`target="_blank"` を付けると推定が静かに死ぬ。** 自分が作った新しいタブでも
visibility は変わるので、mailto が不発でも「開けた」と判定される。退路は
出なくなるが、テストは全件 green のまま通る（＝直したはずの症状が黙って戻る）。
パネルの mailto に `target` を足さないこと。新しいタブで開く面（popup・
ローカル版）は、この理由により**推定を持てない**。ローカル版が Gmail を
常設リンクにしているのはその代償で、手編集した本文を守るには新タブが必須だから。

**`<a href="mailto:…">` は closed shadow root の中に挿して即座に外す。**
href には下書き（氏名・所属・科研費の課題番号）が乗るので、`document.body` に
挿すとホストページから `querySelectorAll('a[href^="mailto"]')` で読める。
パネルを closed shadow DOM に閉じた目的（下記）を丸ごと打ち消す。

**`window.open` は使わない。** `mailto:` を開くと空白タブが残り、コピー経路
（`await clipboard.writeText()` の後）では transient user activation を失って
無言でブロックされうる。今回の症状の原因ではなかったが、実在する問題なので
anchor の click に統一した。

### 学習は利用者のクリックだけを根拠にする

退路の「Gmail で開く」は (a) その場で開く → (b) **開いてから** `mailOpener` を
`gmail` に保存 → (c)「次回から Gmail で開きます（設定で戻せます）」の順。
**推定が外れても設定は書き換えない。** 誤検知のコストを「消せる案内 1 つ」に
留めるため、書き換えの根拠は推定ではなくクリックに置く。

### プライバシー上の書き分け

Gmail を選ぶと下書きが作成画面の URL として Google に渡る。CLAUDE.md の
「外部送信は openBD への ISBN 問い合わせのみ」と衝突して見えるが、これは
**利用者が明示的に選んだ場合の、自分のメール送信先への引き渡し**であって、
このツールからのデータ送信（fetch）ではない（タブ遷移）。新しい権限も不要。
README のプライバシー節にこの区別を書いてある。既定（`auto` / `mailto`）では
渡らない。

## UI / デザインシステム

### 基盤: shadcn/ui (zinc)

配色・角丸・フォーカスリングの語彙は **shadcn/ui の zinc テーマ**に揃える。
ただし `CLAUDE.md` の「ビルドステップを増やさない」に従い、**Tailwind も
shadcn CLI も入れない**。CSS カスタムプロパティで同じトークンを素の CSS として
定義し、コンポーネントは必要な分だけ手で書く。

採用するのはトークンとコンポーネントの「見え方の規約」であって、実装ではない。

### トークン

```
--background --foreground --card --border --input
--primary --primary-foreground --muted-foreground --accent
--destructive --ring --radius
```

- `--primary` は shadcn 既定の zinc-900 ではなく、既存のブランド色
  **ブルー `#1f6feb`** を維持する。生協・書店という「押してよい導線」を
  黒より明示したいため。
- **`--destructive`（赤）はエラーと破壊的操作の専用色。** 「研究費」「注文」
  といった通常情報に赤を使わない。研究費の執行を扱う画面で赤が乱発されると、
  本当の警告が埋もれる。
- ダークモードは `prefers-color-scheme` で切り替える。
  ただし**注入パネル（content.css）は light 固定**とする。注入先ページ
  （Amazon 等）が light 前提のため、そこだけ暗いカードが浮くのを避ける。

### 注入パネルは closed shadow DOM に閉じる

パネルの `<select>` には、利用者が設定した**宛先ラベル**と**財源ラベル（科研費の
課題番号を含む）**が入る。素の `div` としてホストページに append すると、
Amazon 等のページ上で動く任意のスクリプトが
`document.querySelectorAll('#jimoto-panel option')` でそれを読める。
研究費の課題番号がページ側に露出する状態は「個人情報は端末内に留める」と
整合しないため、パネルは `attachShadow({ mode: 'closed' })` の中に入れる。

- **`'open'` は使わない。** `host.shadowRoot` 経由で読めてしまい対策にならない
- manifest の `content_scripts.css` は shadow 境界を越えないため、
  同じ `content.css` を `chrome.runtime.getURL` + `fetch` で読み、
  shadow root に `<style>` として入れ直す。**CSS の実体は `content.css` の
  1 箇所のまま**（JS へ複製しない）。そのため `content.css` は
  `web_accessible_resources` に入れてある
- CSS の取得に失敗してもパネルは表示・動作する（無スタイルになるだけ）。
  ここで throw すると「パネルが出ない」という最悪の壊れ方をする
- **トーストは light DOM のまま。** 表示するのは固定メッセージだけで
  利用者の設定値を含まないため、閉じる必要が無い
- floating フォールバックの `position: fixed` はページのレイアウト上で
  効く必要があるので、`.jimoto-floating` はホスト要素側に付ける

### コンポーネント語彙

増やさない。この 5 つで足りる範囲に UI を収める。

- **button** — variant は `primary`（塗り）/ 既定（outline）/ `ghost`（枠なし）の 3 つ。
  **1 画面の primary は 1 つだけ。** 一次導線がどれか目で分かる状態を保つ。
- **input / select / textarea** — 枠は `--input`、角丸 6px
- **card** — 注入パネルと設定セクションの器
- **toast** — 一時通知。エラーのみ `--destructive` の左ボーダーで区別する
- **focus ring** — `:focus-visible` のみ。マウス操作では出さない

### トークンの単一ソース化（未実装 / 要対応）

現状、同じトークン定義が 3 箇所に重複している。

- `src/adapters/extension/ui.css`（popup / options 用、`:root`）
- `src/adapters/extension/content.css`（注入パネル用、`.jimoto-panel` にスコープ）
- `src/adapters/local/index.html`（インライン `<style>`、`:root`）

`src/core/` を一本化したのと同じ理由で、ここも正を 1 つにする。方針:

1. 正を `src/ui/tokens.css` に置く（`:root` に定義）。
2. ローカル版は `http-server` の root が `src` なので `/ui/tokens.css` を
   `<link>` で参照できる。インライン `<style>` からトークン定義を外す。
3. 拡張は manifest より上を参照できないので、`scripts/sync-core.mjs` が
   `src/ui/` も同期対象に加える。
4. **content script 用だけはスコープ付きに変換して出力する。** 注入先ページに
   `:root { --background: … }` を流し込むと、ホスト側（Amazon）の同名変数を
   壊しうる。sync 時に `:root` → `.jimoto-panel, .jimoto-toast` へ置換した
   `content-tokens.css` を生成する。

これは文字列置換であってバンドルではないので、「ビルドステップを増やさない」には
抵触しない（`sync-core.mjs` の既存の役割の範囲内）。

## スタック方針

「ローカルでも Chrome アドインでも使えるもの」という要求から、次のように決めた。

- **素の ES module。ビルドステップなし。** TypeScript もバンドラも入れない。
  これにより `src/core/*.js` が Node（テスト）・拡張・ローカルページの
  3 環境で無改変のまま動く。
- 唯一の例外が `scripts/sync-core.mjs`。Chrome 拡張は manifest より上の
  ディレクトリを参照できないので、core（と今後は `src/ui/`）をコピーする。
  これはバンドルではなくただのファイルコピーで、依存ゼロ。
- 型が欲しくなったら、TS に移行するのではなく JSDoc + `checkJs` を先に検討する。
- CSS も同じ原則。Tailwind / PostCSS を入れず、素の CSS カスタムプロパティで
  デザイントークンを表現する。

## モジュール境界

```
        ┌──────────────┐
        │  src/core/   │  環境を知らない。Node で単体テストできる
        └──────┬───────┘
               │
     ┌─────────┴──────────┐
     │                    │
┌────▼─────┐      ┌───────▼────────┐
│ extension │      │  local page    │
│  (MV3)    │      │  (http server) │
└───────────┘      └────────────────┘
```

`storage.js` だけが環境を判定する（`chrome.storage` → `localStorage` →
メモリ）。それ以外の core は完全に純粋。

拡張の content script は 5 ファイルに分かれている。`manifest.json` の
`content_scripts[0].js` に
`content-sites.js` → `content-ui.js` → `content-mail.js` → `content-panel.js` →
`content.js` の順で
並べた **classic script** で、ESM ではないためトップレベル宣言を同じ
isolated world で共有する（ファイル間の `import` 文は無い。契約は
`JIMOTO_` / `jimoto` 接頭で grep する）。順序を変えると `JIMOTO_SITES` の参照が
`ReferenceError: JIMOTO_SITES is not defined` で落ちてパネルが静かに出なくなる
（TDZ ではない。別々の classic script は自分自身の instantiation で束縛を作るので、
`content.js` が先に走った時点では束縛がまだ存在しない）。しかも失敗は間欠的で、
`tick()` が `run()` より先に `lastHref` を更新するため同じ URL では再試行せず、
URL が変われば全ファイルがロード済みなので成功する——静的なページロードでは
永久に出ないが、SPA 遷移では 2 つ目の URL から出る。並び順と、実在する
`content*.js` が全部宣言されていることは `test/manifest.test.mjs` が
固定してある。動的 `import()` で分割しないのは、分割した全ファイルを
`web_accessible_resources` に載せることになり、「注入パネルは closed shadow DOM に
閉じる」方針に逆行するため。

| ファイル | 持ち物 |
| --- | --- |
| `content-sites.js` | `JIMOTO_SITES`（サイト別セレクタ）・`jimotoPageText` |
| `content-ui.js` | `jimotoUrl` / `jimotoEl` / `jimotoToast` / `jimotoInjectPanelStyle` |
| `content-mail.js` | `jimotoMakeMailActions` —— `pickMailPlan` → コピー → メーラーの 3 手 |
| `content-panel.js` | `jimotoBuildOrderForm` —— 注文先・支払・財源・冊数の入力欄と `clampQty` |
| `content.js` | パネルの組み立てと差し込み、URL 監視、`attachShadow` |

サイト別の DOM セレクタは `content-sites.js` の `JIMOTO_SITES` 1 箇所に集める。
各サイトの DOM は予告なく変わる前提で、壊れたら該当エントリだけ直せば済む状態を保つ。

## データモデル

### Book

```js
{ isbn13, title, author, publisher, pubdate, price, cover, source }
```

`source` は出所（`openbd` / `page:*` / `empty`）。UI で「書誌未確認」を
出し分けるために持っている。

### Profile

`src/core/profile.js` の `DEFAULT_PROFILE` が正。要点だけ:

- `requester` — 氏名・所属・連絡先・配達場所
- `destinations[]` — 注文先。下記
- `fundingSources[]` — 研究費の財源。`{id, label, code, representative}`。
  複数登録して注文時に選ぶ（科研費と運営費交付金を使い分ける想定）
- `defaults` — 既定の宛先・支払区分・財源・冊数
- `templates` — 文面。大学生協ごとの決まり文句をここで吸収する

**大学が変わっても、コードではなく設定を差し替えれば動く**ことを設計目標に
している。異動・転籍で所属校が変わることは前提に置く。

### Destination — 宛先はリストで持つ

```js
{ id, kind: 'coop' | 'bookstore', label, to, cc,
  receiveMethod, storeName, memberNumber }
```

当初は `coop` / `bookstore` という 2 つの固定枠を持ち、UI では「ルート」を
選ばせていた。これをやめ、**種別つきの宛先リスト 1 本**にした。

- **同じ種別を複数持てないのは実態に合わない。** 地元書店は複数あるし、
  大学が変われば生協も増える。固定枠だと上書きするしかない。
- **テスト用の宛先を本番の宛先と並べて置ける。** 生協の実アドレスが決まる前は
  自分のメールアドレスを宛先として登録して往復させる。固定枠だと本番設定を
  潰すことになり、誤送信の温床になる。
- **UI から「ルート選択」という概念が消える。** ユーザーが選ぶのは
  「どこに注文するか」だけで、生協か書店かは宛先の属性として付いてくる。
  選択の手数が 1 つ減る（→「注文導線」節の方針と揃う）。

`kind` が効くのは 3 箇所だけ。テンプレートの選択（`coopSubject` 系 /
`bookstoreSubject` 系）、研究費・財源の入力行を出すかどうか、
組合員番号を「組合員番号」と呼ぶか「会員番号」と呼ぶか。

`memberNumber` は生協の組合員番号と書店の会員番号を 1 フィールドに統合した。
別々に持つ理由が呼び名しかなかったため。

既定の宛先は `defaults.destinationId`。`fundingSources[]` +
`defaults.fundingSourceId` とまったく同じ形にしてあり、設定 UI も同じ
繰り返しコンポーネントを使う。

**移行**: 旧形式（`coop` / `bookstore` / `defaults.route`）を保存済みの
プロフィールは `withDefaults` が読み込み時に変換する。生成する id を
`'coop'` / `'bookstore'` に固定してあるので、旧 `defaults.route` の値が
そのまま `defaults.destinationId` として通る。ユーザーは既に設定を保存して
いるので、ここが壊れると設定が消える。`test/profile.test.mjs` で固定してある。

**幽霊宛先を作らない**: 旧 `coop.label` / `bookstore.label` は既定値が非空
（`'大学生協 書籍部'` / `'地元書店'`）で、旧 options は `withDefaults()` の出力を
そのまま保存していた。さらに旧 options には書店の `label` 入力欄が無かった。
つまり「label が非空＝ユーザーが設定した枠」とは言えない。そこで移行は
**`to` と `storeName` がどちらも空で、かつ `label` が空またはその種別の旧既定
ラベルと一致する枠は宛先にしない**。旧既定ラベルは `LEGACY_DEFAULT_LABELS`
（`src/core/profile.js`）に定数として置いてある。

この判定が無いと、生協しか設定していなかった人に「地元書店（書店）」が、
書店しか設定していなかった人に「大学生協 書籍部（生協）」が、宛先メール空の
まま生える。`label` があるため設定 UI の保存フィルタ（`d.label || d.to`）も
通過し、手で消さない限り残り続けてしまう。ラベルを自分で書き換えていた人の
情報は失わない（既定ラベルと違えば宛先を作る）。

### 出力（composeOrder の戻り値）

```js
{ to, cc, subject, body, mailto, mailtoHeaderOnly, encodedLength,
  tooLongForMailto, plain, remarks }
```

- `body` — 経路 3 でクリップボードに入るもの（フル版）
- `mailto` — 経路 1・2 で開くもの（本文入り）
- `mailtoHeaderOnly` — 経路 3 で開くもの
- `tooLongForMailto` — 経路の判定。UI はこれだけを見る（長さを再計算しない）
- `plain` — 二次アクション「全文をコピー」用。常にフル版
- `remarks` — 生協の公費 WEB フォームの備考欄にそのまま貼れる 1 行。
  簡略版でも中身は変わらない（本文とは独立）

`compact: true` を渡すと `body` 以下が簡略版になる。キーの形は変わらないので、
UI は同じ扱いのまま 2 通の下書きを `pickMailPlan` に渡せる。

## 未決事項

作業前に確認すること。

1. **最初のターゲット生協はどこか。** 大学が決まると、公費注文の実運用
   （メール宛先か公費 WEB フォームか、備考欄の書式）が確定し、
   `templates` の既定値を実物に合わせられる。今は汎用の仮置き。
2. **地元書店の宛先をどう集めるか。** 手入力で始めているが、
   よく使う書店をプリセットとして同梱するか？
3. ~~**配布形態。**~~ **決定（2026-08-30）: GitHub で公開し、Releases に
   zip を置く。利用者はデベロッパーモードで読み込む。** Chrome ウェブストアには
   出さない（審査を通す前提でスコープを縛られたくない。ストアに出すなら
   単一目的ポリシーと掲載用のプライバシーポリシーが別途要る）。
   プライバシーの説明は README の独立した節に置く。
4. **英語書籍の扱い。** 洋書は生協の洋書ルート（別部署のことが多い）に
   なるが、現状は区別していない。英語テンプレートを持つなら、本文入り
   mailto が長さ制限に収まるので導線が 1 クリック短くなる。
5. **注入パネルのダークモード。** 現状 light 固定。対応サイトが増えて
   dark 前提のページが出てきたら見直す。
6. ~~**メールのボタンを押しても Gmail が立ち上がらない。**~~
   **解決（2026-09-04・利用者が実機で確認）: Gmail が
   `chrome://settings/handlers` で `mailto` のハンドラとして登録されていなかった。**
   未登録だと Chrome は OS の既定メーラーに投げ、それも無ければ無反応になる。
   利用者が既定に設定して解消。**長さは原因ではなかった**（→「メールの開き方」の
   「否定された仮説: 長さ」）。同じ状態の利用者は他にもいるはずなので、
   v0.4 で退路（Gmail 直リンク）と `mailOpener` の設定を入れた。
   ハンドラの登録状況は API で照会できないため、退路と推定以外に手は無い。

## ロードマップ案

- **v0.3 — 宛先リスト化（実装済み・2026-08-30）**
  `coop` / `bookstore` の固定枠を `destinations[]` + `defaults.destinationId` に
  置き換えた（→「Destination」節）。旧プロフィールの移行を含む。
- **v0.4 — 導線とデザインの整理（本 SPEC の「注文導線」「UI」節の実装）**
  1. ~~一次導線を「文面コピー ＋ ヘッダのみ mailto」に統一する~~
     **変更（2026-09-02）: 簡略版の文面を用意し、3 段階（フル版 mailto →
     簡略版 mailto → コピー ＋ ヘッダのみ mailto）で選ぶ方式にした（実装済み）。**
     統一を捨てたのは、簡略版なら本文入り mailto が現実に到達可能で、
     貼り付けを省ける機会を捨てる理由が無くなったため（→「注文導線」節の実測表）
  2. ローカル版の本文自動生成 ＋ ボタン階層の整理
  3. デザイントークンを `src/ui/tokens.css` に単一ソース化
- **v0.5** — e-hon / Honya Club / 丸善ジュンク堂への ISBN ディープリンクを
  「メールの代わりの出力先」として追加。ネットワーク加盟店ならこちらが速い。
- **v0.6** — カーリル API で自大学図書館の蔵書を先に表示する。
  「もう図書館にある」を注文前に見せるのが、研究費の観点で一番効く。
- **v0.7** — 年度末の予算執行アラート（未発注リストの通知）。
- **v0.8** — Firefox 対応（MV3 の差分吸収）。
