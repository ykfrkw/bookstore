# 設計仕様

## 解きたい問題

研究者が本を買うとき、Amazon で見つけてそのまま Amazon で買ってしまう。しかし
実際には、(a) 地元書店で取り寄せれば書店にお金が落ちる、(b) 大学生協なら
研究費で買えて事務処理も通しやすい、という選択肢がある。にもかかわらず、
その 2 つは「Amazon のページから離れて、別のサイトを開いて、書名を打ち直す」
必要があるため選ばれない。

**摩擦の差だけで購買先が決まってしまっている。** ここを埋める。

摩擦を埋めるのが目的なので、**このツール自体の摩擦が数クリックを超えたら本末転倒**
になる。以下の「注文導線」「UI / デザインシステム」は、その制約から逆算して決めている。

## スコープ

### v0.1（実装済み）

- Amazon の書籍ページに注文パネルを注入する
- ISBN を確定し、openBD で書誌を引く
- 生協ルート / 地元書店ルートの注文メール下書きを作る
- 生協ルートでは研究費 / 私費を切り替え、財源（科研費等）と予算代表者を本文に入れる
- 複数冊をまとめて 1 通にする
- 同じロジックがローカルページでも使える

### v0.2（実装済み・2026-08-07）

対応サイトを拡大した。「Amazon のページを改変する拡張」ではなく
「オンライン書店の書籍ページに生協・地元書店ルートを添える拡張」にするのが狙い。

1. 紀伊國屋ウェブストア — URL に ISBN 直埋め込み（`/f/dsg-01-<ISBN13>`）。DOM 依存ほぼゼロ
2. 丸善ジュンク堂ネットストア — 同じく URL 直埋め込み（`/products/<ISBN13>`）
3. 楽天ブックス — URL は内部 ID だが meta タグ・title・本文の 3 経路で ISBN が取れる

honto は 2024 年に紙の通販を終了しているため対象外（後継の丸善ジュンク堂で代替）。
紀伊國屋・丸善ジュンク堂は書誌セレクタを実査していないので、`fallback` は空にして
openBD に任せている（アンカーが外れたら floating 表示になるだけで壊れない）。

### スコープ外（意図的に外している）

- 自動発注・自動ログイン（→ `CLAUDE.md` の禁止事項）
- 図書館蔵書チェック（v0.4 以降）
- 在庫・価格の比較

## なぜ「メール」なのか

当初は各サービスへのディープリンクを考えたが、メールに一本化した。

- **独立系書店には API も注文フォームも無い。** 一番応援したい相手ほど
  デジタル導線が無い。メールなら相手を選ばない。
- **大学生協の公費注文は大学ごとに運用が違う。** 備考欄に財源を書く方式、
  独自の公費 WEB フォーム、外商担当へのメール、と分かれている
  （`docs/research.md` 参照）。最大公約数はメールだった。
- **人の確認が挟まる。** 研究費の執行を伴うので、これは欠点ではなく利点。

ディープリンクは後から「メールの代わりに使える出力先」として足せる設計に
してある（`composeOrder` の戻り値を差し替えれば済む）。

## 注文導線 — 「文面コピー」を一次導線にする

### 前提: 和文の注文メールは mailto に載らない

日本語 1 文字はパーセントエンコードで 9 バイトになる。実測上の安全圏は
URL 全体で 2000 文字前後（`MAILTO_SAFE_LENGTH`）なので、**和文の注文メールは
1 冊でもほぼ確実に超える**。つまり「本文入り mailto でメーラーが開く」経路は
実質使えない。

### 決定

**一次導線は「文面をコピーして、宛先・件名だけのメーラーを開く」1 クリック。**
ユーザーの操作は次の 3 手で終わる。

```
[メールを作る] を押す → メーラーが宛先・件名入りで開く → ⌘V → 送信
```

これを踏まえて、次のように定める。

1. **`tooLongForMailto` による挙動の分岐を UI から外す。**
   長さに関係なく常に「本文をコピー → `mailtoHeaderOnly` で開く」に統一する。
   理由: 和文では分岐の片側（本文入り mailto）がほぼ到達不能な死に経路であり、
   「たまに貼り付けが要らない」挙動はユーザーが学習できない。**一貫して
   貼り付ける方が速い**（迷わないので）。
   判定そのものは `buildMailto` に残す。将来、洋書ルートや英語テンプレートで
   本文入り mailto が現実的になったときに使う。
2. **クリップボードは常に「本文だけ」を入れる。** 貼り付け先がメーラーの本文欄で
   あることが確定しているため、`To:` / `Subject:` 行が混ざると手で消す手間が増える。
   `plain`（ヘッダ込み）を使うのは、メーラーを開かずに Slack 等へ貼る用の
   二次アクション「全文をコピー」だけ。
3. **クリップボード書き込みは必ずフィードバックを出す。** コピーは画面上で
   不可視の副作用なので、成功したか分からないと同じボタンを二度押される。
   toast（`role="status"` / `aria-live="polite"`）＋ボタンラベルの一時変更で示す。
4. **コピーはメーラーを開く前に実行する。** `window.open` でフォーカスが移ると
   `navigator.clipboard.writeText` が拒否されうる。この順序を入れ替えない。

### 二次アクション

一次導線から降ろすが、消さないもの。低強調（ghost）で置く。

| アクション | 中身 | 用途 |
| --- | --- | --- |
| 全文をコピー | `plain`（To/Subject/本文） | メーラーを使わず貼る |
| 備考欄をコピー | `remarks` 1 行 | 生協の公費 WEB フォーム |
| まとめる | カートに追加 | 複数冊を 1 通にする |

### ローカル版の導線

ローカル版には「注文メールを作る」ボタンがあり、押すまで本文が生成されない。
これは中間ステップとして無駄なので、**注文先・支払区分・冊数・ひとことの変更で
本文を自動再生成**し、ボタンは「メールを作る（＝コピーして開く）」に一本化する。
本文 textarea の手編集は引き続き尊重する（`currentDraft` が押下時点の値で
mailto を組み直す方針は維持）。

## UI / デザインシステム

### 基盤: shadcn/ui (zinc)

配色・角丸・フォーカスリングの語彙は **shadcn/ui の zinc テーマ**に揃える。
ただし `CLAUDE.md` の「ビルドステップを増やさない」に従い、**Tailwind も
shadcn CLI も入れない**。CSS カスタムプロパティで同じトークンを素の CSS として
定義し、コンポーネントは必要な分だけ手で書く。

採用するのはトークンとコンポーネントの「見え方の規約」であって、実装ではない。

### トークン

```
--background --foreground --card --border --input
--primary --primary-foreground --muted-foreground --accent
--destructive --ring --radius
```

- `--primary` は shadcn 既定の zinc-900 ではなく、既存のブランド色
  **ブルー `#1f6feb`** を維持する。生協・書店という「押してよい導線」を
  黒より明示したいため。
- **`--destructive`（赤）はエラーと破壊的操作の専用色。** 「研究費」「注文」
  といった通常情報に赤を使わない。研究費の執行を扱う画面で赤が乱発されると、
  本当の警告が埋もれる。
- ダークモードは `prefers-color-scheme` で切り替える。
  ただし**注入パネル（content.css）は light 固定**とする。注入先ページ
  （Amazon 等）が light 前提のため、そこだけ暗いカードが浮くのを避ける。

### 注入パネルは closed shadow DOM に閉じる

パネルの `<select>` には、利用者が設定した**宛先ラベル**と**財源ラベル（科研費の
課題番号を含む）**が入る。素の `div` としてホストページに append すると、
Amazon 等のページ上で動く任意のスクリプトが
`document.querySelectorAll('#jimoto-panel option')` でそれを読める。
研究費の課題番号がページ側に露出する状態は「個人情報は端末内に留める」と
整合しないため、パネルは `attachShadow({ mode: 'closed' })` の中に入れる。

- **`'open'` は使わない。** `host.shadowRoot` 経由で読めてしまい対策にならない
- manifest の `content_scripts.css` は shadow 境界を越えないため、
  同じ `content.css` を `chrome.runtime.getURL` + `fetch` で読み、
  shadow root に `<style>` として入れ直す。**CSS の実体は `content.css` の
  1 箇所のまま**（JS へ複製しない）。そのため `content.css` は
  `web_accessible_resources` に入れてある
- CSS の取得に失敗してもパネルは表示・動作する（無スタイルになるだけ）。
  ここで throw すると「パネルが出ない」という最悪の壊れ方をする
- **トーストは light DOM のまま。** 表示するのは固定メッセージだけで
  利用者の設定値を含まないため、閉じる必要が無い
- floating フォールバックの `position: fixed` はページのレイアウト上で
  効く必要があるので、`.jimoto-floating` はホスト要素側に付ける

### コンポーネント語彙

増やさない。この 5 つで足りる範囲に UI を収める。

- **button** — variant は `primary`（塗り）/ 既定（outline）/ `ghost`（枠なし）の 3 つ。
  **1 画面の primary は 1 つだけ。** 一次導線がどれか目で分かる状態を保つ。
- **input / select / textarea** — 枠は `--input`、角丸 6px
- **card** — 注入パネルと設定セクションの器
- **toast** — 一時通知。エラーのみ `--destructive` の左ボーダーで区別する
- **focus ring** — `:focus-visible` のみ。マウス操作では出さない

### トークンの単一ソース化（未実装 / 要対応）

現状、同じトークン定義が 3 箇所に重複している。

- `src/adapters/extension/ui.css`（popup / options 用、`:root`）
- `src/adapters/extension/content.css`（注入パネル用、`.jimoto-panel` にスコープ）
- `src/adapters/local/index.html`（インライン `<style>`、`:root`）

`src/core/` を一本化したのと同じ理由で、ここも正を 1 つにする。方針:

1. 正を `src/ui/tokens.css` に置く（`:root` に定義）。
2. ローカル版は `http-server` の root が `src` なので `/ui/tokens.css` を
   `<link>` で参照できる。インライン `<style>` からトークン定義を外す。
3. 拡張は manifest より上を参照できないので、`scripts/sync-core.mjs` が
   `src/ui/` も同期対象に加える。
4. **content script 用だけはスコープ付きに変換して出力する。** 注入先ページに
   `:root { --background: … }` を流し込むと、ホスト側（Amazon）の同名変数を
   壊しうる。sync 時に `:root` → `.jimoto-panel, .jimoto-toast` へ置換した
   `content-tokens.css` を生成する。

これは文字列置換であってバンドルではないので、「ビルドステップを増やさない」には
抵触しない（`sync-core.mjs` の既存の役割の範囲内）。

## スタック方針

「ローカルでも Chrome アドインでも使えるもの」という要求から、次のように決めた。

- **素の ES module。ビルドステップなし。** TypeScript もバンドラも入れない。
  これにより `src/core/*.js` が Node（テスト）・拡張・ローカルページの
  3 環境で無改変のまま動く。
- 唯一の例外が `scripts/sync-core.mjs`。Chrome 拡張は manifest より上の
  ディレクトリを参照できないので、core（と今後は `src/ui/`）をコピーする。
  これはバンドルではなくただのファイルコピーで、依存ゼロ。
- 型が欲しくなったら、TS に移行するのではなく JSDoc + `checkJs` を先に検討する。
- CSS も同じ原則。Tailwind / PostCSS を入れず、素の CSS カスタムプロパティで
  デザイントークンを表現する。

## モジュール境界

```
        ┌──────────────┐
        │  src/core/   │  環境を知らない。Node で単体テストできる
        └──────┬───────┘
               │
     ┌─────────┴──────────┐
     │                    │
┌────▼─────┐      ┌───────▼────────┐
│ extension │      │  local page    │
│  (MV3)    │      │  (http server) │
└───────────┘      └────────────────┘
```

`storage.js` だけが環境を判定する（`chrome.storage` → `localStorage` →
メモリ）。それ以外の core は完全に純粋。

サイト別の DOM セレクタは `content.js` の `SITES` 配列 1 箇所に集める。
各サイトの DOM は予告なく変わる前提で、壊れたら該当エントリだけ直せば済む状態を保つ。

## データモデル

### Book

```js
{ isbn13, title, author, publisher, pubdate, price, cover, source }
```

`source` は出所（`openbd` / `page:*` / `empty`）。UI で「書誌未確認」を
出し分けるために持っている。

### Profile

`src/core/profile.js` の `DEFAULT_PROFILE` が正。要点だけ:

- `requester` — 氏名・所属・連絡先・配達場所
- `destinations[]` — 注文先。下記
- `fundingSources[]` — 研究費の財源。`{id, label, code, representative}`。
  複数登録して注文時に選ぶ（科研費と運営費交付金を使い分ける想定）
- `defaults` — 既定の宛先・支払区分・財源・冊数
- `templates` — 文面。大学生協ごとの決まり文句をここで吸収する

**大学が変わっても、コードではなく設定を差し替えれば動く**ことを設計目標に
している。異動・転籍で所属校が変わることは前提に置く。

### Destination — 宛先はリストで持つ

```js
{ id, kind: 'coop' | 'bookstore', label, to, cc,
  receiveMethod, storeName, memberNumber }
```

当初は `coop` / `bookstore` という 2 つの固定枠を持ち、UI では「ルート」を
選ばせていた。これをやめ、**種別つきの宛先リスト 1 本**にした。

- **同じ種別を複数持てないのは実態に合わない。** 地元書店は複数あるし、
  大学が変われば生協も増える。固定枠だと上書きするしかない。
- **テスト用の宛先を本番の宛先と並べて置ける。** 生協の実アドレスが決まる前は
  自分のメールアドレスを宛先として登録して往復させる。固定枠だと本番設定を
  潰すことになり、誤送信の温床になる。
- **UI から「ルート選択」という概念が消える。** ユーザーが選ぶのは
  「どこに注文するか」だけで、生協か書店かは宛先の属性として付いてくる。
  選択の手数が 1 つ減る（→「注文導線」節の方針と揃う）。

`kind` が効くのは 3 箇所だけ。テンプレートの選択（`coopSubject` 系 /
`bookstoreSubject` 系）、研究費・財源の入力行を出すかどうか、
組合員番号を「組合員番号」と呼ぶか「会員番号」と呼ぶか。

`memberNumber` は生協の組合員番号と書店の会員番号を 1 フィールドに統合した。
別々に持つ理由が呼び名しかなかったため。

既定の宛先は `defaults.destinationId`。`fundingSources[]` +
`defaults.fundingSourceId` とまったく同じ形にしてあり、設定 UI も同じ
繰り返しコンポーネントを使う。

**移行**: 旧形式（`coop` / `bookstore` / `defaults.route`）を保存済みの
プロフィールは `withDefaults` が読み込み時に変換する。生成する id を
`'coop'` / `'bookstore'` に固定してあるので、旧 `defaults.route` の値が
そのまま `defaults.destinationId` として通る。ユーザーは既に設定を保存して
いるので、ここが壊れると設定が消える。`test/profile.test.mjs` で固定してある。

**幽霊宛先を作らない**: 旧 `coop.label` / `bookstore.label` は既定値が非空
（`'大学生協 書籍部'` / `'地元書店'`）で、旧 options は `withDefaults()` の出力を
そのまま保存していた。さらに旧 options には書店の `label` 入力欄が無かった。
つまり「label が非空＝ユーザーが設定した枠」とは言えない。そこで移行は
**`to` と `storeName` がどちらも空で、かつ `label` が空またはその種別の旧既定
ラベルと一致する枠は宛先にしない**。旧既定ラベルは `LEGACY_DEFAULT_LABELS`
（`src/core/profile.js`）に定数として置いてある。

この判定が無いと、生協しか設定していなかった人に「地元書店（書店）」が、
書店しか設定していなかった人に「大学生協 書籍部（生協）」が、宛先メール空の
まま生える。`label` があるため設定 UI の保存フィルタ（`d.label || d.to`）も
通過し、手で消さない限り残り続けてしまう。ラベルを自分で書き換えていた人の
情報は失わない（既定ラベルと違えば宛先を作る）。

### 出力（composeOrder の戻り値）

```js
{ to, cc, subject, body, mailto, mailtoHeaderOnly, encodedLength,
  tooLongForMailto, plain, remarks }
```

- `body` — 一次導線でクリップボードに入るもの
- `mailtoHeaderOnly` — 一次導線で開くもの
- `plain` — 二次アクション「全文をコピー」用
- `remarks` — 生協の公費 WEB フォームの備考欄にそのまま貼れる 1 行
- `mailto` / `tooLongForMailto` — 現状 UI からは使わない。洋書・英語テンプレ用に残す

## 未決事項

作業前に確認すること。

1. **最初のターゲット生協はどこか。** 大学が決まると、公費注文の実運用
   （メール宛先か公費 WEB フォームか、備考欄の書式）が確定し、
   `templates` の既定値を実物に合わせられる。今は汎用の仮置き。
2. **地元書店の宛先をどう集めるか。** 手入力で始めているが、
   よく使う書店をプリセットとして同梱するか？
3. ~~**配布形態。**~~ **決定（2026-08-30）: GitHub で公開し、Releases に
   zip を置く。利用者はデベロッパーモードで読み込む。** Chrome ウェブストアには
   出さない（審査を通す前提でスコープを縛られたくない。ストアに出すなら
   単一目的ポリシーと掲載用のプライバシーポリシーが別途要る）。
   プライバシーの説明は README の独立した節に置く。
4. **英語書籍の扱い。** 洋書は生協の洋書ルート（別部署のことが多い）に
   なるが、現状は区別していない。英語テンプレートを持つなら、本文入り
   mailto が長さ制限に収まるので導線が 1 クリック短くなる。
5. **注入パネルのダークモード。** 現状 light 固定。対応サイトが増えて
   dark 前提のページが出てきたら見直す。

## ロードマップ案

- **v0.3 — 宛先リスト化（実装済み・2026-08-30）**
  `coop` / `bookstore` の固定枠を `destinations[]` + `defaults.destinationId` に
  置き換えた（→「Destination」節）。旧プロフィールの移行を含む。
- **v0.4 — 導線とデザインの整理（本 SPEC の「注文導線」「UI」節の実装）**
  1. 一次導線を「文面コピー ＋ ヘッダのみ mailto」に統一する
  2. ローカル版の本文自動生成 ＋ ボタン階層の整理
  3. デザイントークンを `src/ui/tokens.css` に単一ソース化
- **v0.5** — e-hon / Honya Club / 丸善ジュンク堂への ISBN ディープリンクを
  「メールの代わりの出力先」として追加。ネットワーク加盟店ならこちらが速い。
- **v0.6** — カーリル API で自大学図書館の蔵書を先に表示する。
  「もう図書館にある」を注文前に見せるのが、研究費の観点で一番効く。
- **v0.7** — 年度末の予算執行アラート（未発注リストの通知）。
- **v0.8** — Firefox 対応（MV3 の差分吸収）。
